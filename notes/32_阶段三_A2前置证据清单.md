# 32｜阶段三 A2-small 前置证据清单

状态：`DESIGN_ONLY_NOT_AUTHORIZED`。A2-small、第二次 smoke、训练、真实 3D 重建与渗透率计算均未获授权。

本清单把既有 `stage3_branch_a_a2_metric_interface_schema_20260706.json` 转为可签核的前置项；任何缺项均为 fail-closed，而不是补写结论。

`src/hy7_stage3_a2_preflight.py` 只验证轻量声明的完整性。即使其通过，也仅表示 `READY_FOR_FUTURE_GATE_REVIEW_ONLY`，并硬编码 `execution_authorized=false`；它不读取体素、不生成 phantom、也不授权 A2。

## 输入身份与尺度桥接

- [ ] 每个 2D 输入的 source path、完整 SHA256、shape、dtype、轴序、voxel spacing 与 slice range。
- [ ] 明确 ct28/Amics/Maps/FIB 的空间关系：同一物理样品、配准变换、误差/不确定性与不可配准区域。
- [ ] 定义条件变量的每个 channel、物理单位、缺失策略、来源、时间/深度坐标及为何可作为条件；不得只给数组名称。
- [ ] 明确 B1.1 formal route=`threshold_formal_full_batch`、planning anchor=`ep015_all`；qmatch route=`nnUNet ep015_qmatch` 仅为 diagnostic；`ep015_chunk384_447` 仅 triage，失败 chunk `ep015_chunk000_063` 必须可见。

## 指标实现与 phantom

- [ ] 3D phi、6 邻域 connected porosity、x/y/z percolation、最大连通簇双分母均已实现并有 known-answer tests。
- [ ] S2 固定 non-periodic valid-pairs 或其他显式 convention；完成 asymmetric-boundary phantom。
- [ ] 若 Euler/Minkowski 用于 gate/结论，先完成实现及 phantom 验证；否则保持 `explicitly_de_scoped_fail_closed`。
- [ ] 完成 all-solid、all-pore、isolated voxel、三个轴向直通道、two blobs、cavity/ring、asymmetric S2、seeded sparse toy，在 `8^3/16^3/32^3` 的确定性 phantom 套件。
- [ ] 未把 2D penetrate、连通代理或 flow proxy 写为 3D permeability。

## 预注册 A2-small 协议

- [ ] fresh strict gate verdict 明确允许一次 A2-small，指定算法族、体尺寸、seed、停止规则和禁止事项。
- [ ] package 包含 A2 manifest、input slice manifest、algorithm config、route constraints、2D/3D metrics、语义说明、provenance、negative evidence 和 hashes。
- [ ] 大体积/权重不入 Git；外部 artifact 记录 path、size、SHA256、command、environment、preview。
- [ ] 明确任何输出仍为 `diagnostic_only/not_evidence`，除非新 gate 单独批准更高表述。

## 当前裁定

当前缺少跨尺度配准身份、条件变量定义、完整 3D phantom 验证和 fresh execution gate。因此不得启动 A2-small。
