# Local cleanup archive — 2026-07-06

This folder holds local entries moved out of the repository root to reduce Finder/workspace confusion.

Moved entries:

- `vectorstore/` — empty directory at cleanup time (`entries=0`).

Policy:

- No real data was deleted.
- Empty or obsolete local entries are archived first instead of removed directly.
- If a future workflow needs vector search, create a new documented vectorstore with a README and reproducible build command.
