# 31｜B1.1 冻结候选受控验证预注册卡

状态：`PRE_REGISTERED_NOT_AUTHORIZED`。本卡不授权训练、采样、推理、远程写入或新 checkpoint。

## 目的与边界

仅在研究负责人后续明确授权时，对已冻结的 `ep015` 做一次无训练的受控验证；验证其强制 qmatch 路径在固定协议下是否保持可复查。不得把结果升级为 B1.1 unconditional pass、B2 final pass、3D 数字岩心或数字井筒结论。

- 固定候选：`ep015`；不重训、不新增 checkpoint、不做第二次 topology rescue。
- 固定校准：`hy7-gray-calibration-qmatch-v1`；ORIG raw 始终并列记录为 `known_fail`，不得作为通过路线。
- 路线不可合并：threshold formal full-batch 与 nnUNet-qmatch diagnostic 分开报告。
- 已有 B2-min package/selection/handoff 是 design/dry-run evidence，不替代本卡的未来授权。

## 冻结输入

| 项目 | 固定值或要求 |
|---|---|
| checkpoint | 远程 `ddpm_ct28_gray_b11_eulermaxcc_rescue20_formal64_20260706/ckpt_ep015.pt`；运行前记录完整 SHA256 |
| 灰度测试集 | `slices_ct28_gray_128/test.npy`；记录路径、shape、完整 SHA256 |
| 孔隙参考 | `slices_ct28_gray_128/test_pore.npy`；记录路径、shape、完整 SHA256 |
| qmatch reference | 明确 reference path、split=`all/even/odd`、完整 SHA256；不得事后换 reference |
| 代码 | 已提交基线 SHA `90a654f` 或经批准的后续提交；记录工作树是否干净 |
| 环境 | Python、torch、diffusers、nnUNet、CUDA/GPU、命令、cwd |

## 固定协议

- sampling seed：`123`；样本量：`n=512`；size=`128`；batch size 与硬件限制一并记录。
- formal threshold evaluation：`phi_target=6.4`、`rmax=48`、evaluation seed=`0`。
- formal gate：`S2 rmse <= 0.003`、`phi in [5.5, 6.8]`、`Euler >= 115`、`maxCC <= 0.070`，四项必须同时通过。
- nnUNet-qmatch diagnostic：必须记录 phi、S2、Euler、maxCC、Dice/IoU（如适用）和 `reverse_fail`；该路线不替代 formal route。
- qmatch generalization：固定 even/odd reference split；两者都必须保留，不得只报较优一侧。

## 停止与判读

1. 输入 SHA、qmatch version、代码 SHA 或命令与本卡不一致：停止，写 `NOT_RUN_PROTOCOL_MISMATCH`。
2. 任何 formal gate 不通过：停止后续升级，仅归档失败 evidence；不得调阈值、seed、权重或重采样以追结果。
3. ORIG raw 未通过不构成 qmatch 路径失败的新结论，但必须保留其 `known_fail` 状态。
4. 即使所有指标通过，结论仅为“该冻结候选在本预注册协议下完成受控验证”；仍需独立研究 gate 才能授权任何 B2 实质实验。

## 授权前检查清单

- [ ] 研究负责人明确批准本卡的单次无训练验证。
- [ ] 本卡、输入/环境 manifest、命令和预期输出目录已提交或不可变存档。
- [ ] 远程部署 SHA 审计通过，且仅使用声明的 `phase2/` runtime 目标。
- [ ] 输出目录为空或为新的带日期目录；不覆盖历史 evidence。
- [ ] 回传仅限轻量 JSON/MD/hash/小图；数组、权重不入 Git。
